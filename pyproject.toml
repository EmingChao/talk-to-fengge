[project]
name = "deepseek-talk-to-me"
version = "0.1.0"
description = "实时语音 Agent：用叶会羽的人格和记忆进行语音对话，并沉淀记忆。"
requires-python = ">=3.11,<3.15"
dependencies = [
    "aiohttp>=3.10,<4",
    "httpx>=0.28,<1",
    "livekit-agents[google]>=1.5,<1.6",
    "livekit-plugins-cartesia>=1.5,<1.6",
    "numpy>=1.26,<3",
    "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
